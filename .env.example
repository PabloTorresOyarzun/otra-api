# Tokens externos para el sistema SGD
BEARER_TOKEN=tu-bearer-token-aqui

# Azure Document Intelligence
AZURE_ENDPOINT=https://tu-azure-endpoint.cognitiveservices.azure.com/
AZURE_KEY=tu-azure-key-aqui

# ============================================
# AUTENTICACIÓN API
# ============================================
# Tokens permitidos para acceder a la API REST (separados por comas)
# Ejemplo: API_TOKENS=token1,token2,token3
# NOTA: Este método es legacy. Se recomienda usar el gestor de tokens (tokens.json)
# Para generar tokens seguros, puedes usar:
#   - Python: import secrets; secrets.token_urlsafe(32)
#   - OpenSSL: openssl rand -base64 32
#   - Online: https://www.uuidgenerator.net/
API_TOKENS=mi-token-de-prueba,otro-token-para-testing

# Token de administrador para gestionar tokens (endpoints /admin/tokens/*)
# Este token permite crear, listar y eliminar tokens de la API
# IMPORTANTE: Mantén este token secreto y seguro
ADMIN_TOKEN=admin-super-secreto-cambiame

# ============================================
# CONFIGURACIÓN OPCIONAL (con valores por defecto)
# ============================================

# HTTP Timeouts (segundos)
# TIMEOUT_CONNECT=5.0
# TIMEOUT_READ=30.0
# TIMEOUT_WRITE=10.0

# Azure DI Timeouts
# TIMEOUT_AZURE_BASE=60
# TIMEOUT_AZURE_PER_PAGE=2
# TIMEOUT_AZURE_MAX=600

# Excel Conversion Timeouts
# TIMEOUT_EXCEL_BASE=30
# TIMEOUT_EXCEL_PER_MB=5
# TIMEOUT_EXCEL_MAX=300

# Retry Configuration
# MAX_RETRIES=3
# RETRY_BACKOFF_MIN=2.0
# RETRY_BACKOFF_MAX=10.0

# File Size Limits (MB)
# MAX_FILE_SIZE_MB=100
